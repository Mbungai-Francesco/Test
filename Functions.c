int BodyMassIndex(int w, float h)
{ float BMI;
  BMI=w/(h*h);
return BMI;
}
int Idealweight()
{ float iw, h;
  iw=25*(h*h);
  return iw;
}
int swap(int a[i],int)
{
  int result;
  if ()
}
double getaverage(double a[], int size)
{
  int i;
  double avg, sum;
  for(i=0;i<size;i++)
  {
    sum+i=a[i];
  }
  avg=sum/size;
  return avg;
}
